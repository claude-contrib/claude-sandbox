services:
  claude:
    volumes:
      - "${CLAUDE_SSH_AUTH_SOCK}:${CLAUDE_SSH_AUTH_SOCK}"
      - "${CLAUDE_HOST_HOME}/.ssh/known_hosts:${CLAUDE_HOST_HOME}/.ssh/known_hosts"
    environment:
      - SSH_AUTH_SOCK=${CLAUDE_SSH_AUTH_SOCK}
